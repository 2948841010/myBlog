# 只在 npm run dev 时加载（Vite 此时 mode = development）。
#
# 线上构建走 production 模式，读不到这个文件，VITE_ENABLE_STUDIO 就是 undefined，
# 写作台连同它的代码一起不会进入产物 —— 这是刻意的默认：线上关闭。
#
# 如果哪天你想在线上也打开写作台，在托管平台（如 Vercel）的环境变量里
# 加一条 VITE_ENABLE_STUDIO=on 并重新部署即可。
VITE_ENABLE_STUDIO=on
